# ============================================================================
# AUTHENTICATION OPTIONS (Choose ONE method below)
# ============================================================================

# OPTION 1: Google Cloud Vertex AI (Recommended for production)
# --------------------------------------------------------------
# Requires: gcloud auth application-default login
# Best for: Production deployments, team projects, enterprise use
GOOGLE_GENAI_USE_VERTEXAI=True
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# OPTION 2: Google AI Studio (API Key)
# --------------------------------------
# Requires: Get API key from https://aistudio.google.com/apikey
# Best for: Quick prototyping, personal projects, local development
# Uncomment and set these instead of Option 1:
# GOOGLE_GENAI_USE_VERTEXAI=False
# GOOGLE_API_KEY=your-google-api-key-here

# ============================================================================
# AGENT CONFIGURATION
# ============================================================================

# Model to use for the RepoReviver agent
# Options: gemini-2.5-flash, gemini-2.0-flash-exp, gemini-1.5-pro, gemini-1.5-flash
REPO_REVIVER_MODEL=gemini-2.5-flash

# ============================================================================
# GITHUB AUTHENTICATION (for Codespaces)
# ============================================================================

# LOCAL DEVELOPMENT:
# ------------------
# Uses GitHub CLI (gh) authentication - run these commands once:
#   gh auth login
#   gh auth refresh -h github.com -s codespace
#
# Verify with: gh auth status

# CLOUD DEPLOYMENT (Agent Engine / Cloud Run):
# ---------------------------------------------
# GitHub CLI won't be authenticated in cloud environments.
# You must provide a Personal Access Token (PAT):
#
# 1. Create PAT at: https://github.com/settings/tokens
#    Required scopes: codespace (full control)
# 2. Set as environment variable in cloud deployment config:
#    GH_TOKEN=ghp_your_personal_access_token_here
#
# The gh CLI will automatically use GH_TOKEN if available.
